function twoSum(nums, target) {
  // go through array choose num (go through array again and see if any other numbers add to the target)
  // if (not) then go again till i find a match
  let firstNum;

  for (let i = 0; i < nums.length; i++) {}
}

twoSum([2, 7, 11, 15], 9);
